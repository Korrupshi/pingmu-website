---
interface Props {
	title: string;
}
import '../styles/global.css';   
import NavBar from '../components/NavBar.astro'
import Footer from '../components/Footer.astro'

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/flavicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link href="https://fonts.googleapis.com/css2?family=Outfit&family=Plus+Jakarta+Sans&display=swap" rel="stylesheet">
		<title>{title}</title>
		 <script defer>
			if (window.location.hash === '#privacy') {
				window.location.href = '/privacy';
			}

			if (window.location.hash === '#terms') {
				window.location.href = '/terms';
			}

			if (window.location.hash === '#delete-data') {
				window.location.href = '/delete';
			}
		</script>
	</head>
	<body>
	<NavBar/>

		<slot />
	<Footer/>

	</body>
</html>

<!-- Below needed for navbar padding -->
<!-- <style>
	body{
		padding-top: 88px;
	}
</style> -->
